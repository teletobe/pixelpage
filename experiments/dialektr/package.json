{
  "name": "dialektr",
  "version": "0.1.0",
  "description": "Österreichische Dialektwörter erraten — run with: npm install && npm run dev",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "vite": "^5.0.0"
  }
}
